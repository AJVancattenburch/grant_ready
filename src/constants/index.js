import { db } from './db/index.js';

export const nav_links = db.nav_links;
export const hero = db.hero;
export const about = db.about;
export const service_section = db.service_section;
export const testimonial_section = db.testimonial_section;